version: '3'

services:
  db:
    image: postgres
  web:
    build: ./django_project
    command: python /code/manage.py runserver 0.0.0.0:8000
    volumes:
      - ./django_project:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
    container_name: web